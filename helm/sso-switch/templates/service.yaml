apiVersion: v1
kind: Service
metadata:
  name: {{ include "sso-switch.fullname" . }}
  labels:
    {{- include "sso-switch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sso-switch.selectorLabels" . | nindent 4 }}
